// Stub de acceso a datos. Solo existe para que el ejercicio del Bloque A
// sea ejecutable. No representa una implementacion real.

function ejecutar(consulta) {
  return { consulta, filas: [] };
}

module.exports = { ejecutar };
